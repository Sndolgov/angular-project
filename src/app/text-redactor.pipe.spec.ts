import { TextRedactorPipe } from './text-redactor.pipe';

describe('TextRedactorPipe', () => {
  it('create an instance', () => {
    const pipe = new TextRedactorPipe();
    expect(pipe).toBeTruthy();
  });
});
